= render "rewards_form", :action => "Update"
%p#delete-link= link_to "Delete Reward", @reward, :method => :delete, :confirm => "Are you sure?  This cannot be undone!", :title => "Delete #{@reward.name}"
